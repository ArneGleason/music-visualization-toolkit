# Astronomer outro A1 v3: one performance retry, base only

Read this folder's handoff.json and paste this folder's flow_prompt.txt verbatim with its unchanged lock. Upload first_frame.png, the SAME approved image as v2. No end frame, no audio upload. Do not execute the old v2 instructions or overwrite its media/receipt.

The owner saw v2. The required change is MISSION ACCOMPLISHED rather than grateful, broadly smiling happiness. Confident colleague, dry familiarity, a quiet acknowledging nod, real open articulation but no grin during the later sung run. Do not solve this by making her deadpan or closed-mouth. The television's brief static response and reflected light on her face are now ALLOWED and potentially useful, not a reason to reject or retry. Keep its geometry stable and unobstructed for later graphics.

Generate exactly ONE Veo3.1 Quality Frames take,8seconds,16:9,720p,x1. Expected100credits; verify actual UI before submission. Stop if unavailable or more expensive. No purchases, model substitutions or automatic retries. No Kling, B1 or A2. No assembly or commit.

Check after30seconds, then about every20seconds, or10seconds at90%/near completion. Count tool time. Do not use exponential backoff or wait minutes after a near-complete status. Never submit duplicates. Download promptly when ready.

Return full untouched original under its download filename plus a byte-identical base.mp4 in THIS folder. Preserve native fps, all frames and audio, no trim/retime/crop. Inspect start/middle/end, especially later smiles, question visibility, mouth activity and listening gaps. Return even imperfect results with observations; no autonomous retry. Write RECEIPT.md with paths, hashes, native fps, decoded frames, dimensions, duration, actual settings/credits and job ID. Add claude_result to handoff.json and set status to claude_done_ready_for_codex_verification only after files are present; record a blocker if not.

Timing remains EXACTLY as v2:24fps, zero-based end-exclusive song4165..4357,192frames. First visible cut4177..4232 uses source12..67; return4262..4331 uses source97..166. Source time continues while offscreen; never restart. Possible later A2 anchor is source155, not permission to generate it or pick a different frame silently. Approximate phrase directions are not a verified Kling guide. Codex will review the base, then prepare fresh sync instructions separately.
