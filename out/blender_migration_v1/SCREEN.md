# Screen acquisition into the garden

Native shot16 migration rendered, technically checked and owner-approved.
Do not promote based only on the existence of this note.

[Numbered preview, shots15..17](C:/Users/arneg/OneDrive/Documents/ChatGPT/music-visualization-toolkit-video-generation/out/blender_migration_v1/screen_context.mp4)

All103 clean frames and275 preview frames decoded and counted at24fps. Seven
1080 samples passed resolution checks (largest per-frame mean difference1.25
levels/255 after diagnostic downsampling). Three saved-scene rerenders matched
the sequential output pixel-for-pixel. Both saved scenes use only the original
receiver/screen plates as image dependencies. Source hash and tracking checks
passed. See screen/qa.json, blend_audit.json and receipts.json.

Visual checks covered acquisition, expansion, full takeover, higher-resolution
expansion and the numbered contextual contact sequence. Owner playback review
accepted this look. Prior encoded-reference differences are diagnostics only.

Delivery: song [1201,1304),103frames at24fps. Preview [1137,1412),275frames,
includes approved native receiver shot15 and untouched raw garden shot17.
No changed cuts, camera speed, lyrics or music offsets.

The screen image is cropped from the original interference footage, acquired
inside the tracked circular receiver screen and expanded on deal1246. Full
image takeover is1258. Garden begins1304. All masks, texture mapping, curve
geometry, blur, screen expansion and compositing run in Blender. Extracted
PNGs contain original video pixels only, not rasterized effects.

The saved screen scene includes12 history frames before delivery, preserving
the accepted message curve trails. Render scene13..115 inclusive. Two ordered
geometry samples per frame retain the prior light animation. The full-frame
takeover entirely hides the receiver source hold after1258; the screen video
continues advancing, with no visible frozen image.

Scripts: prepare_screen.py, screen_native.py, screen_fields.py, run_screen.py,
audit_screen.py and check_screen.py. The native recipe deliberately reuses the
accepted message recipe; do not disable view layers based on cached visibility.

Isolated screen_shotlist.json extends message_shotlist.json. Shared assembly
source resolver and prior protected overrides remain active. Garden uses raw
forest_threshold_v6.mp4 frames[0,108), not the old baked composite. Native
screen caches are for convenient review assembly, not a claim that the final
film already embeds every native effect scene.

Validation targets: all103 output frames, exact24fps, decode/receipt checks,
seven1080 samples, saved-scene random access25/64/70, and source-only image
dependencies. Compare encoded prior appearance diagnostically, not as an exact
pixel-equivalence requirement. Review screen_context.mp4 with numbers15..17.

No paid generation, upscaling, mastering, production manifest edit or Git push.
