# One next shot: Then they don't quite match

Read handoff.json first. Paths are relative to project_root. Do not generate later shots.

We have approved the opening through the new close-up and the "pieces" detail
cutaway. Next is a short, clear failed-fit action, not a repeat of the same sorting.
Existing spare footage does not provide enough unused safe action: only14 frames
remain in opening_specimen_v2 after its preceding use; the old v6 take develops
an unwanted viewpoint morph after the region used for the approved detail.

## Execute

1. Verify input hashes. Use codex/out/canal_map_macro_b.jpg as the sole first-frame
   reference, no end frame. Do not use extracted Flow video frames as inputs.
2. Submit ONE eight-second Veo3.1 Quality Frames take,16:9,720p,x1. Paste
   flow_prompt.txt verbatim, including its unchanged lock. Expected100 Flow credits.
   Stop if the model/settings are unavailable or the quoted cost is higher.
3. Download the untouched original and a byte-identical base.mp4. No Kling:
   hands only. Do not trim, slow, loop, interpolate or assemble the result.
4. Inspect all eight seconds for stable overhead framing, credible fingers and
   rigid pieces. Locate the interval where a different edge is compared, visibly
   fails to fit and is drawn apart. The preferred excerpt is source2.0..4.041667s,
   but REPORT actual action timing rather than claiming the prompt guaranteed it.
   Need49 consecutive usable frames at24fps. A tiny imperfection can be reported;
   a morph, camera change, hidden mismatch or repeated reset is not a clean result.
5. Write RECEIPT.md: files/hashes, job ID, settings/cost, fps/frame count, duration,
   stream starts, usable intervals, clearest failed-fit moment, final prop state,
   and any weaknesses. Preserve original download and add claude_result to manifest.
   Set status claude_done_ready_for_codex_verification after files are saved.
   If unusable or blocked, say so explicitly; no automatic paid retry.

## Editorial contract, not a request for Claude to assemble

Song cut670..719 at24fps =27.916667..29.958333 seconds. Lyric680..719 is fully
contained. The following phrase starts719, so do not extend this shot across it
merely to obtain a longer tail. Full source handles stay on disk for later review.
The candidate source48..97 maps its source zero to song622. This is an unsynced
prop insert; no vocal offset, no audio guide, no pre-roll synchronization math.
Codex will choose the final source window and build the next same-clock preview.

## Scope and finish

Write only inside out/specimen_mismatch_v1/. Do not edit production shotlist,
cues, global registers or prior handoffs. No commits, no further generation.
Do not mark owner-approved or final. Tell the owner when manifest and receipt
are ready for Codex verification. We are progressing one reviewed shot at a time.
