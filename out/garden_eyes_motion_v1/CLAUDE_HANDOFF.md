# Garden eyes wake: one clean motion plate

Read handoff.json first. Work inside its project_root. Owner requested new motion
for the static eyes-in-the-dark scene. This is NOT lip sync.

1. Verify first-frame hash. Attach approved forest_fauna_eyes_a.jpg as sole FIRST
frame, no end frame. Do not use an extracted video frame or alter the still.
Paste flow_prompt.txt verbatim, including its unchanged style lock.
2. One Flow Veo 3.1 Quality / Frames /16:9 /720p /8seconds /x1. Expected100 credits
from previous jobs, not a live quote. Check the actual quote and stop above100
or if settings unavailable. No paid retries or alternate model. NO Kling.
3. Check at30seconds, then20seconds; at90% or above check every10seconds.
With rising percentages, estimate remaining time from recent progress and check
halfway to that estimate, clamped10..20seconds. No progressive backoff. Tool
latency counts; no overlapping checks, repeated reloads or background services.
Honor service rate limits/retry-after. At15minutes report status. Download promptly
when ready; never resubmit to fix an uncertain download.
4. Preserve untouched original plus byte-identical base.mp4. Do not crop, trim,
retime, stabilize, add particles or assemble. Full take and real handles matter.
5. Inspect full motion: actual head/creature movement plus layered foliage motion,
not just eye flickering, blinking or a camera move. Each eye pair stays attached
to its own stable partially hidden creature. Keep reference colours, darkness,
occlusions and creature count. No morphing, floating eyes, newly exposed bodies,
horror action or generated particle effects. In particular check source0.5..3.875
for visibly living action. Report weaknesses, do not spend on a retry.
6. Write RECEIPT.md with original/base paths, SHA-256, native fps/frame count,
dimensions/durations/stream starts, job ID, actual cost, input hashes, camera
behaviour, useful interval and any defects with timestamps. Include progress/
submission/ready/download times. Add claude_result, preserving other manifest
fields, and set claude_done_ready_for_codex_verification once saved. If blocked,
record the stage/reason instead.

## Clock and ownership

Proposed revised shot is song1569..1650 at24fps, the whole phrase
"Something with no name is waking". Source0 maps to song1557; source12..93
is the visible shot, giving12 real lead frames. Requested192frames would leave99
exit frames; report actual useful coverage rather than assuming it. No audio
guide needed for this non-speaking plate. Do not use old s019 timing1527..1595.

Codex will inspect and reapply the accepted three-depth-band luminous airflow,
plus suitably restrained musical eye/light responses if useful, using freshly
sampled song timing and updated motion masks. Do not bake those effects into Flow.
Existing protected static composite remains the assembly fallback pending explicit
approval of the moving replacement. Preserve old media as spares.

Write ONLY under out/garden_eyes_motion_v1/. No other shots, production changes,
commits or paid retries. Tell the owner when Codex can check manifest and receipt.

