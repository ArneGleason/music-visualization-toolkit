# Authorized replacement, not another attempt to join the pieces

Read this folder's handoff.json. The owner rejected the v1 action in playback:
it looks like a successful fit followed by snapping apart, with a reported
growing-back glitch around5s. Your original receipt and media are preserved.
Do not argue usability from the old candidate window or overwrite the old take.

Submit exactly ONE new Flow take with this folder's flow_prompt.txt pasted
verbatim, including the unchanged lock. Use the manifest's approved still as
FIRST frame only, no end frame or generated-video-frame reference. Veo3.1 Quality,
Frames,16:9,720p,8 seconds,x1. Expected100 credits; stop if higher or unavailable.
The new action is separated fingertip rotations with a gentle overhead dolly
push. The lock allows slow symmetrical push-ins; do not reintroduce the old
fixed-camera instruction or failed-fit choreography. No Kling, no audio guide.

Check normal generation status after about60 seconds, then about every25 seconds
where the browser workflow permits. Honor any service rate limits. Download
promptly when ready, not after a long fixed sleep. Never duplicate a submission
because it is pending or its download is uncertain.

Inspect the full result, especially around5 seconds: no joining, snapping,
shape change, new lobes, duplication, hand exits or camera angle change. Find
49 consecutive clean frames of turning separated pieces. Report actual action
times, camera behavior and weaknesses honestly. Source48..97 is a candidate,
not a mandatory trim. Do not retime, loop, crop or assemble to hide a failure.

Save the untouched download plus byte-identical base.mp4 in this folder. Write
RECEIPT.md with filenames, SHA-256, actual native fps/frame count/duration,
job ID, credits and usable intervals. Add claude_result and change status to
claude_done_ready_for_codex_verification after files are saved. Flag an unusable
result explicitly. No automatic second retry.

Write only in out/specimen_mismatch_v2/. No production edits, no commits, no
later shots. Codex will review and assemble song670..719 with the unchanged
master. This retry is authorized; its result is not pre-approved.
