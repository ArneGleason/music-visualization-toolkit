# Lip-sync follow-up after opening review

The owner approves the beginning-through-meant preview overall and requests two improvements:

1. Opening: fresh sync of the accepted original slower performance, to compare naturalness. Keep the old synced take as fallback.
2. Specimen: add lip sync on "I'm sure it's there. Hard to find, right?" at song frames2059..2122. Earlier listening remains unchanged. Reapply the approved artifact glow after verification.

Run each subfolder's handoff.json once using Kling only. No Flow, automatic retries, cut changes, or production assignment. The supplied guides already include the exact source-clock alignment; upload from time zero with no extra offset.

Historical expected cost is10credits per job,20total. Verify current UI and stop if higher. Check progress after30seconds, then approximately every20seconds or10near completion, respecting service limits. Download promptly when ready.

On completion save untouched downloads, receipts and hashes, and update each manifest's status and claude_result. Codex will verify and produce short comparisons before adoption.
